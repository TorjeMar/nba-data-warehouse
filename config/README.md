# Configuration

Place future non-secret configuration here, such as:
- database connection templates
- pipeline settings
- environment-specific defaults

Secrets should continue to live outside version control.
